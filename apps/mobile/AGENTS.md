# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Keep Expo SDK 54 while development depends on Expo Go on physical devices.
Only upgrade to SDK 57 together with the adoption of a development build.
